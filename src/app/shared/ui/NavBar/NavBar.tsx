import { AppBar } from "@mui/material";
import { LogoSvg } from "../../../assets/images";

export const NavBar = () => {
  return (
    <AppBar position={"relative"}>
      <LogoSvg />
    </AppBar>
  );
};
